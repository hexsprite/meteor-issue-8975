describe('test', () => {
  it('should work', () => {
    const lolex = require('lolex')
    var clock = lolex.install()
    clock.uninstall()
    // clock = lolex.install()
    // clock.uninstall()
  })
})
